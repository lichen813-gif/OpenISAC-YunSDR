# OpenISAC Windows VLC 视频信道仿真使用说明

## 1. 已实现链路

```text
VLC 文件输入 -> MPEG-TS/UDP 127.0.0.1:50000
-> UDP 数据报分片
-> CRC16 + LDPC(1008,504) + 可选1x1 SISO、2x2 Rank-2、4x4 Rank-2/4或2Tx Alamouti STBC
-> 可配置的1x1、2x2或4x4 TDL多径 + AWGN + CFO + SFO
-> ZC 同步 + 信道估计 + SISO/MMSE/STBC检测 + 软解调 + LDPC/CRC
-> UDP 数据报重组 -> 127.0.0.1:50001 -> VLC 播放
```

视频字节确实经过完整物理层和信道模型。任一PHY分片CRC失败时，对应的完整UDP数据报会被丢弃，不会把错误字节送给VLC。第一版不使用ARQ重传。

## 2. 当前物理层

| 项目 | 参数 |
|---|---:|
| FFT / CP | 1024 / 128 |
| 采样率 / 占用带宽 | 15.36 Msps / 13.44 MHz |
| 帧 / 导频模式 | 默认`fdm`：1个ZC前导 + 2个数据符号，225 us；可选`nr-dmrs`：ZC + 2个前置DM-RS + 2个数据符号，375 us |
| 天线模式 | 1Tx/1Rx SISO Rank-1；空间模式支持2Tx/2Rx Rank-2、4Tx/4Rx Rank-2/4；STBC为单流2Tx/2Rx Alamouti |
| 调制 / 检测 | QPSK、16/64/256-QAM；SISO单抽头均衡、Rank-2/4 MMSE、STBC两时隙合并；MRC仅保留在双端口控制头和内部算法中 |
| 4端口Rank-2 | 固定半酉4x2 DFT预编码，4x4物理CSI转4x2等效信道，4Rx联合两层MMSE |
| FEC / 校验 | LDPC(1008,504) / CRC16 |
| PHY用户载荷 | 随模式/MCS变化；64-QAM下STBC为376、SISO为439、4端口Rank-2为565、2x2 Rank-2为880、Rank-4为1132字节 |
| 视频分片头 | 20字节，从上述PHY用户载荷中扣除 |

典型的1316字节MPEG-TS UDP包在64-QAM 2x2 Rank-2/4x4 Rank-4模式占用两个PHY帧，在4端口Rank-2模式占用三个PHY帧，在SISO和STBC模式占用四个PHY帧。程序保留UDP数据报边界和顺序。

### 2.1 完整模式支持矩阵

| 正式模式 | Rank | MIMO模式参数 | Tx/Rx端口 | 数据流数 | 映射与接收算法 | FDM | NR-DMRS | 感知链路 |
|---|---:|---|---:|---:|---|:---:|:---:|---|
| SISO | 1 | `spatial` | 1/1 | 1 | Tx0直接映射，Rx0单抽头均衡 | 支持 | 支持 | Tx0→Rx0 |
| Alamouti STBC | 1 | `stbc` | 2/2 | 1 | 两时隙2Tx Alamouti，2Rx联合合并 | 支持 | 支持 | 当前选择Tx0→Rx0 |
| 2×2空间复用 | 2 | `spatial` | 2/2 | 2 | 两层直接映射，2Rx MMSE | 支持 | 支持 | 当前选择Tx0→Rx0 |
| 4×4 Rank-2 | 2 | `spatial` | 4/4 | 2 | 固定半酉4×2 DFT预编码，4Rx×2 MMSE | 支持 | 支持 | 16条物理链功率合并 |
| 4×4 Rank-4 | 4 | `spatial` | 4/4 | 4 | 四层直接映射，4Rx×4 MMSE | 支持 | 支持 | 16条物理链功率合并 |

所有正式模式均支持QPSK、16-QAM、64-QAM和256-QAM，以及TDL多径、AWGN、CFO、SFO、定时偏移、LDPC(1008,504)、CRC16、UDP视频分片、实时星座/时域/信道图和FER统计。

以下组合会被入口直接拒绝：`2×2 spatial Rank-1`、`4×4 Rank-1`、`2×2 Rank-4`、端口数不相等，以及除2×2 Rank-1之外的`stbc`组合。底层双端口Rank-1/MRC代码仍用于控制头和内部算法，但不是正式视频载荷模式。

帧头携带Rank、调制方式和`spatial/stbc`传输模式；物理Tx/Rx端口数及`fdm/nr-dmrs`导频模式属于收发双方预先约定的带外配置。Windows仿真由同一个命令同时配置收发端，不会失配；后续接入真实设备时必须保证两端参数一致。

### 2.2 各模式每PHY帧用户载荷

下表单位为字节，已经扣除PHY CRC16占用，但尚未扣除视频分片头。每个视频PHY分片还要从表中减去20字节，因此应用数据容量为“表中数值−20”。FDM和NR-DMRS每帧载荷相同；NR-DMRS仅增加帧时长。

| 模式 | QPSK | 16-QAM | 64-QAM | 256-QAM |
|---|---:|---:|---:|---:|
| 1×1 SISO Rank-1 | 124 | 250 | 439 | 565 |
| 2×2 Alamouti STBC Rank-1 | 124 | 250 | 376 | 502 |
| 2×2空间复用 Rank-2 | 250 | 565 | 880 | 1195 |
| 4×4 DFT预编码 Rank-2 | 187 | 376 | 565 | 754 |
| 4×4空间复用 Rank-4 | 376 | 754 | 1132 | 1510 |

FDM帧为225 us，NR-DMRS帧为375 us。在载荷相同的情况下，NR-DMRS理论PHY净吞吐为FDM的60%。表中容量是PHY编码容量，不等于VLC码率；实际视频还受到MPEG-TS、UDP、20字节分片头、CRC失败丢包和主机处理速度影响。

## 3. 首次使用

先构建并执行全部C++测试：

```bat
cd /d C:\path\to\OpenISAC-YunSDR
cpp_phy\build_windows_vs2019.cmd
```

运行不依赖VLC的逐字节完整链回环测试：

```bat
cpp_phy\test_video_channel_sim.cmd
```

看到 `PASS`、`FER 0.000%` 和输入/输出包数相同，说明UDP分片、PHY、信道、CRC和重组工作正常。

## 4. 一键播放

推荐使用新的手动参数入口。双击或在终端执行：

```bat
cpp_phy\manual_video_phy_test.cmd
```

命令会依次询问视频文件、Rank、调制方式、SNR、CFO、SFO、定时偏移、含多普勒的多径、图形刷新周期、感知相干帧数、发射相关系数、接收相关系数、四端口转码码率、导频模式、MIMO模式、Tx端口数和Rx端口数。MIMO模式可选`spatial`或`stbc`；STBC必须设置Rank-1和2/2端口。所有模式都会打开前台VLC接收画面、C++物理层日志和Python实时监视器；4端口Rank-2显示两层星座、4×4物理信道响应和16链路感知结果。

### 4.1 手动命令完整格式

```text
manual_video_phy_test.cmd <VideoFile> [Rank] [Modulation] [SnrDb] [CfoHz]
    [SfoPpm] [TimingSamples] [Tdl] [RefreshSeconds]
    [SensingCoherentFrames] [TxCorrelation] [RxCorrelation]
    [VideoBitrateKbps] [PilotMode] [MimoMode] [TxPorts] [RxPorts]
```

参数是严格的位置参数。若要设置后面的参数，前面的参数也必须按顺序给出；视频路径、TDL字符串和任何包含空格的值必须用双引号括起来。

| 位置 | 参数 | 默认值 | 可选值/范围 | 对视频传输的作用 |
|---:|---|---|---|---|
| 0 | `VideoFile` | 自动搜索测试MP4 | 本机存在的视频文件 | VLC输入文件；建议使用绝对路径并加双引号 |
| 1 | `Rank` | `2` | `1`、`2`、`4` | 数据流数；SISO/STBC用1，2×2/4×4双流用2，4×4四流用4 |
| 2 | `Modulation` | `64QAM` | `QPSK`、`16QAM`、`64QAM`、`256QAM` | 阶数越高，每帧载荷越大，但对SNR、EVM和信道估计更敏感 |
| 3 | `SnrDb` | `45` | 有限dB值 | AWGN信噪比；建议从45 dB开始，再逐步降到40/36/32 dB观察FER |
| 4 | `CfoHz` | `300` | 有限Hz值，可正可负 | 注入载波频偏，接收端用CP估计并校正 |
| 5 | `SfoPpm` | `20` | 工程建议绝对值不超过20 ppm | 注入采样率偏差，接收端估计相位斜率并校正 |
| 6 | `TimingSamples` | `20` | `0`～`128` | 在帧前加入采样点偏移，用于验证ZC定时同步 |
| 7 | `Tdl` | 见下文 | 至少一径；最大时延小于128点CP | 配置每径时延、功率、初相和可选多普勒 |
| 8 | `RefreshSeconds` | `2` | `0.2`～`3600`秒 | C++生成星座、时域、信道、EVM和感知快照的周期 |
| 9 | `SensingCoherentFrames` | `128` | `0/16/32/64/128/256/512` | 距离–速度相干积累帧数；`0`关闭感知，数值越大速度分辨率越高、刷新越慢 |
| 10 | `TxCorrelation` | `0.2` | `-0.95`～`0.95` | 发射侧空间相关系数；仅MIMO/STBC有工程意义，SISO建议设`0` |
| 11 | `RxCorrelation` | `0.2` | `-0.95`～`0.95` | 接收侧空间相关系数；高绝对值会恶化MIMO条件数，SISO建议设`0` |
| 12 | `VideoBitrateKbps` | `1000` | `250`～`5000` kbit/s | 仅4端口模式启用VLC H.264转码限速；1/2端口模式直接复用原视频流，此值不生效 |
| 13 | `PilotMode` | `fdm` | `fdm`、`nr-dmrs` | `fdm`为225 us短帧；`nr-dmrs`增加两个前置训练符号，帧长375 us |
| 14 | `MimoMode` | `spatial` | `spatial`、`stbc` | `spatial`用于SISO/空间复用；`stbc`仅用于2×2 Rank-1 Alamouti |
| 15 | `TxPorts` | `0`自动 | `0/1/2/4` | 物理发射端口数；必须与模式矩阵匹配 |
| 16 | `RxPorts` | `0`自动 | `0/1/2/4` | 物理接收端口数；当前必须等于Tx端口数 |

端口自动选择规则：`spatial + Rank-1`自动为1/1，`Rank-4`自动为4/4，其他合法模式自动为2/2。4×4 Rank-2必须显式写`4 4`，否则Rank-2默认选择2×2。

手动脚本还会自动设置以下运行参数，无需在17个位置参数中重复填写：

| 项目 | 脚本设置 |
|---|---|
| VLC发送端 → PHY | `127.0.0.1:50000`，MPEG-TS/UDP |
| PHY → VLC接收端 | `127.0.0.1:50001`，MPEG-TS/UDP |
| FFT / CP / 子载波间隔 | `1024 / 128 / 15000 Hz`，当前不可改成其他组合 |
| UDP入口队列 | 8192个数据报 |
| LDPC线程 | 1/2端口为8线程，4端口为12线程 |
| MMSE加载系数 | `0.5`，用于4端口检测 |
| 空间信道随机种子 | `49239`，保证相同配置可重复比较 |
| 遥测目录 | `measurement\live_phy_monitor` |
| 时域快照长度 | 最多4096个复采样点 |
| 感知导出距离bin | 128个非负距离bin |
| 感知中心频率 | 5.8 GHz，用于多普勒到速度换算 |
| VLC接收缓存 | 500 ms |
| 发送端处理 | 4端口转码为H.264＋96 kbit/s音频；1/2端口只封装并发送原流 |

TDL单径格式为：

```text
时延采样:功率dB:初始相位度[:多普勒Hz]
```

多径用`+`连接。例如：

```text
0:0:0:0+3:-14:45:0+9:-8:-80:69.444444
```

它表示0、3、9采样点三径，功率分别为0、−14、−8 dB，末径多普勒为69.444444 Hz。在5.8 GHz单站感知换算下，该多普勒约对应1.795 m/s径向速度。第四字段省略时多普勒为0 Hz。

### 4.2 实时图形输出

SISO/2端口实时窗口包含：

- SISO/STBC的单流星座图或空间Rank-2的两层星座图；星座图只显示实际LDPC编码覆盖的QAM符号，帧尾固定标签0的填充RE不会再堆积在右上角；
- 一帧完整接收时域I/Q波形，默认最多4096点；
- H00/H01/H10/H11信道估计幅频响应；
- EVM、信道NMSE、频偏估计、SFO残余、定时估计、FER，以及2x2/4x4信道条件数中位值/P90和病态子载波比例；
- 由实际已知发送网格和接收网格得到的距离-速度热力图、CFAR目标标记、距离与径向速度。

SISO窗口只绘制H00信道响应，并把空间条件数显示为`SISO 1×1`；距离–速度处理使用唯一的Tx0→Rx0链路。

四端口实时窗口中，Rank-2显示两层星座、Rank-4显示四层星座；信道图显示H00/H11/H22/H33四条物理对角响应，CSV保留全部16条链路；距离–速度图使用16链路逐链变换后的功率合并结果。4端口Rank-2显示预编码后4×2等效信道的条件数，Rank-4显示4×4物理信道条件数。

### 4.3 模式命令示例

也可用完整位置参数直接启动而不询问：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 2 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2
```

Rank-4视频仿真示例：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 4 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2 1000
```

启用统一DM-RS帧的Rank-4示例（最后一个参数为`nr-dmrs`）：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 4 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.02 0.02 1000 nr-dmrs
```

Alamouti STBC视频仿真示例（最后两个参数为导频和MIMO模式）：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 1 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2 1000 fdm stbc
```

1Tx/1Rx SISO视频仿真示例：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 1 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0 0 1000 fdm spatial 1 1
```

4Tx/4Rx Rank-2视频仿真示例（最后两个参数显式指定4个Tx/Rx端口）：

```bat
cpp_phy\manual_video_phy_test.cmd "C:\path\to\video.mp4" 2 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2 1000 nr-dmrs spatial 4 4
```

最后五个参数均可从命令尾部整体省略并使用默认值；不能跳过中间参数后直接填写更后面的参数。4端口模式自动使用12个LDPC线程并将VLC输入转码到参数12指定的码率，其他模式使用8个LDPC线程且不执行该转码。

### 4.4 运行、结束与自动清理

手动脚本在测试期间会保持命令窗口运行并管理它启动的全部子进程。出现以下任一情况时测试结束：

- 发送端播放完整个视频并退出；
- 用户关闭接收端VLC；
- 物理层桥接程序退出；
- 用户在手动脚本命令窗口按 `Ctrl+C`。

发送端正常播完后，脚本先等待物理层遥测至少静默3秒（或图形刷新周期加1秒，取较大值），让入口队列和VLC的500 ms网络缓存排空，避免截断视频尾部。随后脚本请求正常关闭，1.5秒内未退出时再强制关闭本次测试创建的发送VLC、接收VLC、Python实时监视器和物理层桥接器。它按进程ID清理，不会关闭用户在测试前已经打开的其他VLC实例。用户主动关闭接收VLC或按 `Ctrl+C` 时不等待排空，直接清理。

安装VLC后执行：

```bat
cpp_phy\start_vlc_video_demo.cmd "D:\video\sample.mp4" 1500 45
```

三个参数依次是视频文件、视频码率kbit/s和SNR dB。音频固定为128 kbit/s。程序会打开信道统计窗口、VLC接收窗口和VLC发送窗口。

推荐第一轮使用1500 kbit/s视频和45 dB SNR。确认稳定后逐步把SNR降低到40、36、32 dB，观察FER、画面块效应和停顿。

如果暂时不准备视频文件，可直接播放程序生成的10秒动态测试图案：

```bat
cpp_phy\start_vlc_test_pattern_demo.cmd 45
```

测试图案为320x180、15 fps、约800 kbit/s H.264。发送和接收VLC都强制使用独立实例，避免VLC把两端合并到同一进程。

## 5. 分别启动

先启动信道：

```bat
cpp_phy\run_video_channel_sim.cmd --snr 45 --cfo 300 --sfo 20 --tdl 0:0:0+3:-4:45+9:-8:-80 --tx-correlation 0.2 --rx-correlation 0.2
```

再启动接收VLC：

```bat
cpp_phy\start_vlc_video_receiver.cmd
```

最后启动发送VLC：

```bat
cpp_phy\start_vlc_video_sender.cmd "D:\video\sample.mp4" 1500
```

默认端口：VLC发送到50000，解调成功的数据发往VLC接收端50001。按 `Ctrl+C` 停止信道程序。

## 6. 信道参数

```text
--snr 45
--cfo 300
--sfo 20
--timing 20
--tdl 0:0:0+3:-4:45+9:-8:-80
--tx-correlation 0.2
--rx-correlation 0.2
--spatial-seed 49239
--rank 2
--tx-ports 4
--rx-ports 4
--mimo-mode spatial
--modulation 64QAM
--pilot-mode nr-dmrs
--queue-packets 8192
--telemetry-dir measurement\live_phy_monitor
--telemetry-interval 2
--telemetry-points 4096
--sensing-coherent 128
--sensing-range-bins 128
```

`--rank` 接受1、2、4；`--tx-ports`和`--rx-ports`接受1、2或4并且当前要求二者相等。正式组合为1x1 Rank-1 SISO、2x2 Rank-1 STBC、2x2 Rank-2空间复用、4x4 Rank-2空间复用和4x4 Rank-4空间复用。端口参数省略时，空间Rank-1自动选1/1、Rank-4自动选4/4，其余自动选2/2。`spatial + 2x2 + Rank-1`不再是可选模式。

4端口Rank-2使用固定半酉DFT预编码矩阵，两个数据层同时映射到全部四个Tx端口。四端口导频先估计完整4×4物理信道，接收端再计算`H_eff = H × W`并以4Rx×2层MMSE检测。帧头仍只携带Rank-2；物理端口配置属于收发机带外配置，因此发送端和接收端必须使用相同的4/4端口设置。

`--mimo-mode`接受`spatial`和`stbc`，默认`spatial`。`stbc`固定与`--rank 1`组合，使用两个连续数据OFDM符号形成2Tx Alamouti码块，并由两个接收通道联合合并；它是单流发射分集，不等同于Rank-2空间复用。控制头的两位传输模式字段为`00/01/10/11 = Rank-1/Rank-2/Rank-4/STBC`，接收端按帧内标志自动选择解码路径。

`--pilot-mode`接受`fdm`和`nr-dmrs`，默认`fdm`保持旧命令及225 us帧不变。`nr-dmrs`在ZC后增加两个连续训练符号，SISO、STBC、2×2和4×4均使用同一五符号帧。SISO的两个训练符号均由Tx0发射；STBC在双物理端口架构中探测两个Tx端口；4×4把端口0/1和2/3放到两个交织频域梳齿，每个梳齿内用双符号时域OCC分离端口。数据符号保留原稀疏参考，用于噪声估计和前置CSI相位跟踪。该结构与NR思路相似，但不是逐比特兼容3GPP的PDSCH DM-RS实现。

SISO同条件显式对比（64-QAM、45 dB、300 Hz CFO、20 ppm SFO、动态三径，固定种子首帧）中，FDM/NR-DMRS均逐字节恢复且FER为0。FDM帧为3个OFDM符号/225 us，EVM 1.846%、CSI NMSE -25.179 dB；NR-DMRS帧为5个符号/375 us，EVM 3.828%、CSI NMSE -25.191 dB。两者每帧载荷相同，因此DM-RS理论净空口吞吐为FDM的60%。本模型只有低速运动且数据符号仍带稀疏参考，前置DM-RS的CSI到数据区会发生时间老化，所以DM-RS不保证改善EVM；它的主要收益是形成SISO/MIMO统一、端口正交的训练结构。

TDL每条路径格式为 `时延采样:功率dB:初始相位度[:多普勒Hz]`，多条路径使用 `+` 连接。第四字段可省略，省略时为静态路径，因而完全兼容原三字段命令。最大路径时延必须小于128点CP。单站模型的速度换算为 `v = fd*c/(2*fc)`；5.8 GHz下69.444 Hz约为1.795 m/s，负值表示反向径向速度。

空间相关采用低复杂度 Kronecker 模型：每条TDL路径先生成独立的2x2复高斯矩阵，再按 `Rrx^(1/2) * W * Rtx^(1/2)` 引入收发两侧相关性。`--tx-correlation` 和 `--rx-correlation` 的绝对值必须小于1；0表示独立天线，0.2是推荐默认值，0.6用于明显相关场景，0.9接近病态阵列，只适合压力测试。`--spatial-seed` 控制可重复的信道 realization，相同TDL、相关系数和种子可复现实验。该实现仅涉及每条路径上的2x2小矩阵乘法，适合后续C++实时实现，不增加迭代求解。

当前视频入口没有 `--condition-number` 或“目标条件数”参数。Tx/Rx相关系数只能改变条件数的统计分布，不能精确指定监视器中显示的条件数；`--spatial-seed` 固定后可重复得到同一个信道实现，适合比较不同接收算法。若要精确扫描条件数，需要使用后续规划的受控奇异值诊断信道，而不是把相关系数误当成条件数。

监视器中的“条件数 中位/P90”用于判断空间流是否可分离。条件数越接近1越好；程序把条件数大于10的有效子载波记为病态子载波。高相关性或随机信道的小奇异值都会使空间信道趋于不可分，MMSE噪声增强加重，即使设置很高的SNR，EVM仍可能明显变差。这是Rank-2/4 MIMO信道可分离性限制，不是AWGN数值设置失效。SISO和单流STBC不使用空间条件数判据。

视频桥每个刷新周期只采集一个连续感知批次，其余时间不执行距离-多普勒FFT。默认128帧相干积累在`fdm`下为28.8 ms、多普勒bin间隔34.722 Hz、速度bin间隔0.897 m/s；在`nr-dmrs`下为48.0 ms、20.833 Hz和约0.539 m/s。两种模式的距离bin间隔均为9.759 m；静态杂波均值抑制和二维CA-CFAR默认开启。4端口Rank-2/4均对16条物理Tx→Rx链路分别完成距离/多普勒变换，再进行功率非相干合并，不要求跨射频链相位校准。

`constellation.csv` 只保存当前分片实际使用的星座符号。`status.csv` 中的 `constellation_valid_symbols` 是绘制点数，`constellation_padding_symbols_excluded` 是未绘制的帧尾填充数。短UDP分片不再因为大量标签0填充而在64-QAM右上角显示一团；该改动只影响诊断显示，不改变发送波形、LDPC、CRC或接收判决。

程序每两秒显示：UDP输入/输出/丢弃包数、输入/输出码率、PHY帧数、FER和每帧处理延迟。由于VLC自身还有转码和网络缓存，播放器端到端延迟通常大于PHY处理延迟。

FFT、CP和子载波间隔可在命令行显式写为 `--fft 1024 --cp 128 --subcarrier-spacing 15000`，但正式帧算法目前只接受这一组参数；传入其他数值会立即报错，而不会静默使用错误参数。

## 7. 当前限制

- Rank和调制方式已可手动选择，但尚未在视频过程中自动切换MCS。
- SISO视频链路已支持1Tx/1Rx Rank-1、FDM/NR-DMRS、四种调制、TDL/CFO/SFO、LDPC/CRC、实时绘图及单链路距离–速度感知。
- 4端口Rank-2/4已支持视频字节、星座、16条信道响应及距离–速度感知闭环，但仍是Windows离线信道仿真，不代表能在`fdm`的225 us或`nr-dmrs`的375 us空口帧周期内实时完成。
- C++视频链路已支持独立的2Tx/2Rx Alamouti STBC模式，兼容FDM和NR-DMRS以及全部四种调制。当前不支持4Tx正交STBC，也未实现视频过程中在空间复用与STBC之间自动切换。
- UDP接收使用独立线程和8192包有界队列，图形窗口同时显示Socket接收量、队列丢包、当前深度和峰值。队列只吸收突发；如果平均输入长期超过PHY处理能力，延迟仍会持续增长。
- 当前没有ARQ。低SNR下CRC失败的数据报直接丢弃，符合低延迟视频验证需求。
- 当前输出距离和径向速度，尚未做阵列测角；TDL多普勒是帧间相位旋转的窄带目标模型，不模拟目标微动或路径出生/消失。
- VLC已在本机标准路径完成联调；脚本也会搜索PATH。

## 8. 当前实测

- SISO视频回环：64-QAM、45 dB、300 Hz CFO、20 ppm SFO和含69.444 Hz多普勒的三径下，FDM与NR-DMRS自动回归均逐字节恢复且FER为0。NR-DMRS显式测试完成6个UDP包、22个PHY帧，6/6恢复、FER 0，并生成单流星座、H00、时域和16帧感知数据；固定种子首帧的FDM/NR-DMRS EVM为1.846%/3.828%，CSI NMSE为-25.179/-25.191 dB。
- 4Tx/4Rx Rank-2回环：64-QAM、45 dB、300 Hz CFO、20 ppm SFO、相关系数0.2和含69.444 Hz多普勒的三径下，FDM与NR-DMRS各20个UDP包、68个PHY帧均逐字节恢复且FER 0；平均完整仿真处理约3.463/4.474 ms每帧。NR-DMRS再以50个UDP包、170个PHY帧和128帧感知积累测试，FER 0、EVM 4.802%、等效4×2信道条件数中位/P90为2.764/3.143，感知峰值87.830 m、62.5 Hz（1.615 m/s），CFAR目标1个。
- STBC视频回环：Rank-1/64-QAM、45 dB、300 Hz CFO、20 ppm SFO、Tx/Rx相关系数0.2和含69.444 Hz多普勒的三径条件下，FDM与NR-DMRS各20个UDP数据报、100个PHY帧全部逐字节恢复，FER均为0；平均PHY处理时间分别约0.251/0.335 ms每帧。NR-DMRS再以128帧相干积累测试30个UDP包、150个PHY帧，FER 0、EVM 3.529%，感知峰值87.830 m、62.5 Hz（1.615 m/s，受20.833 Hz Doppler分辨率量化），CFAR目标1个。
- 统一DM-RS兼容回归：空间Rank-2/4、64-QAM、45 dB、300 Hz CFO、20 ppm SFO、Tx/Rx相关系数0.02时，各30个UDP数据报均逐字节恢复，FER均为0；对应PHY帧数为72和60。Rank-4固定种子100帧对照中，`fdm`的EVM/预FEC BER/CSI NMSE/接收均值为6.562%/0.005047/-25.736 dB/838.996 us，`nr-dmrs`为7.082%/0.004446/-25.683 dB/986.250 us，两者均100/100 CRC通过。无CFO/SFO的30帧隔离测试中，梳齿+时域OCC DM-RS的EVM和CSI NMSE为5.823%和-45.982 dB，优于FDM的7.378%和-40.890 dB；实际20 ppm条件下前置CSI仍有时间老化，因此当前版本以兼容性和BER闭环为通过判据，不宣称EVM必然优于FDM。
- `nr-dmrs`保持每帧载荷不变，但帧由225 us增加到375 us，所以理论净空口吞吐为原`fdm`的60%。这是第一版保守结构；后续若去掉数据符号中的重复信道导频，可回收部分开销，但必须先保留相位跟踪和噪声估计能力。

- 新增Rank-4视频字节回环：Rank-4/64-QAM、45 dB、300 Hz CFO、20 ppm SFO、空间相关三径、12个LDPC线程，30个UDP数据报拆为60个PHY帧，30/30逐字节恢复，FER 0，平均仿真处理约3.892 ms/PHY帧。Rank-2兼容回归也通过20/20数据报，FER为0。
- Rank-4视频感知综合回归：80个UDP数据报、160个PHY帧全部逐字节恢复，FER 0；128帧、16链路功率合并报告87.830 m、69.444 Hz、1.795 m/s，CFAR输出1个显示目标；四层星座各756点、时域波形3508点、16条信道CSV及Python监视器读取全部通过。

- VLC桌面捕获实播：74个MPEG-TS UDP包、148个PHY帧，FER 0、UDP丢包0。
- 动态I420/H.264测试图案：132个MPEG-TS UDP包、264个PHY帧，FER 0、UDP丢包0。
- 两轮均使用45 dB SNR、300 Hz CFO、20 ppm SFO和默认三径TDL。
- 36 dB扩展回环：50个UDP包、120个PHY帧，逐字节一致，FER 0；该短样本只作为工程回归，不代替长时间统计。
- `1920x1080_2Mbps_2.mp4`：26789个UDP包、53578个PHY帧，输入输出一致，FER 0、桥内丢包0，接收流水约0.390 ms/帧。
- `1920x1080_5Mbps_2.mp4` 初测：桥内47719个UDP包、95438个PHY帧全部通过，FER 0、桥内丢包0，接收流水约0.374 ms/帧；但VLC日志发现3859次TS连续计数跳变，证明旧版同线程UDP入口在高码率突发下发生统计范围外丢包。该结果判定为“PHY正确、端到端未通过”，并据此加入独立UDP接收队列；修复后的复测结果应另行记录，不能用桥内FER代替端到端连续性检查。
- 独立UDP接收队列修复后，同一5 Mbit/s源复测：Socket收到52983包，队列峰值6958/8192、队列丢包0；52983包全部恢复输出，105966个PHY帧FER 0，接收流水约0.367 ms/帧；VLC TS连续性错误为0。该样片端到端通过，但队列峰值已达到容量约85%，更高码率或更强突发应增大队列或继续优化发送/信道流水。
- 动态感知闭环：默认运动路径为9采样延迟、-8 dB、69.444 Hz。80个UDP数据报拆为192个Rank-2/64-QAM PHY帧后全部逐字节恢复，FER 0；128帧感知报告87.830 m、69.444 Hz、1.795 m/s，CFAR最强目标与注入参数一致。
- 星座填充回归：188字节短包对应672个有效64-QAM符号和1760个帧尾填充符号；CSV只输出672个有效点，右上角点比例由约45.6%恢复到1.49%，该帧CRC通过。短视频生命周期测试结束后，本次脚本创建的两个VLC、Python监视器和物理层桥接器进程全部自动退出。
- 空间相关扫频（Rank-2/64-QAM、45 dB、相同种子、收发相关系数取相同值）：平坦TDL下相关系数0/0.2/0.6/0.9对应EVM约0.91%/0.92%/1.55%/6.29%，条件数中位值约2.57/2.58/4.65/19.64；0.9时100%有效子载波条件数大于10。默认动态三径、相关系数0.2时EVM约2.60%、条件数中位/P90约2.77/4.27，FER 0。结果确认相关性参数和病态信道诊断方向正确。
- 空间相关综合回归（Rank-2/64-QAM、45 dB、Tx/Rx相关系数0.2、300 Hz CFO、20 ppm SFO）：80个UDP包、192个PHY帧全部逐字节恢复，FER 0；快照EVM 2.02%、信道NMSE -26.09 dB、条件数中位/P90为2.78/4.21、病态子载波0%；感知峰值仍为87.830 m、1.795 m/s。

## Rank-4 EVM低复杂度优化

Rank-4接收端默认对两个已经完成相位对齐的数据OFDM符号的CSI做帧内相干平均，并把导频残余噪声用于MMSE时采用0.5倍正则化系数。可用视频桥参数 `--mmse-scale VALUE` 或诊断程序参数 `--mmse-scale VALUE --intra-frame-average 0|1` 做固定种子对照。手动视频脚本已显式传入 `--mmse-scale 0.5`。

在Rank-4/64-QAM、45 dB、300 Hz CFO、20 ppm SFO、Tx/Rx相关系数0.02和相同三径TDL下，500帧串行VS2019 Release对照如下：未优化EVM 6.938%、预FEC BER 0.005709；优化后EVM 6.605%、预FEC BER 0.005088，500/500帧CRC全部通过。EVM相对改善约4.8%，BER改善约10.9%，接收均值从810.9 us增加到824.5 us。

同条件视频快照中，Rank-2的EVM约2.29%、条件数中位/P90约2.75/4.41、病态子载波0%；Rank-4的EVM约5.58%、条件数中位/P90约7.71/15.71、病态子载波约32.25%。Rank-4保持总发射功率不变，因此每层功率比Rank-2低3 dB，并且每层要分离另外三个空间层，这是两者EVM差距的主要原因。Rank-2仿真入口使用已知SNR噪声方差，而Rank-4使用导频残余估计，也会造成统计口径上的次要差异。
