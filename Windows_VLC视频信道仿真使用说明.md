# OpenISAC Windows VLC 视频信道仿真使用说明

## 1. 已实现链路

```text
VLC 文件输入 -> MPEG-TS/UDP 127.0.0.1:50000
-> UDP 数据报分片
-> CRC16 + LDPC(1008,504) + 可选 Rank-1/2/4 与 QPSK/16/64/256-QAM OFDM
-> 可配置空间相关的2x2或4x4 TDL多径 + AWGN + CFO + SFO
-> ZC 同步 + 信道估计 + MRC/MMSE + 软解调 + LDPC/CRC
-> UDP 数据报重组 -> 127.0.0.1:50001 -> VLC 播放
```

视频字节确实经过完整物理层和信道模型。任一PHY分片CRC失败时，对应的完整UDP数据报会被丢弃，不会把错误字节送给VLC。第一版不使用ARQ重传。

## 2. 当前物理层

| 项目 | 参数 |
|---|---:|
| FFT / CP | 1024 / 128 |
| 采样率 / 占用带宽 | 15.36 Msps / 13.44 MHz |
| 帧 / 导频模式 | 默认`fdm`：1个ZC前导 + 2个数据符号，225 us；可选`nr-dmrs`：ZC + 2个前置DM-RS + 2个数据符号，375 us |
| MIMO | Rank-1/2使用2x2端口；Rank-4使用4x4端口，均为空间复用 |
| 调制 / 检测 | QPSK、16/64/256-QAM；Rank-1 MRC、Rank-2/4 MMSE |
| FEC / 校验 | LDPC(1008,504) / CRC16 |
| PHY用户载荷 | 随Rank/MCS变化；Rank-2/64-QAM为880字节，Rank-4/64-QAM为1132字节 |
| 视频分片头 / 数据 | 20字节；上述两种模式分别最多860/1112字节视频数据 |

典型的1316字节MPEG-TS UDP包会占用两个PHY帧。程序保留UDP数据报边界和顺序。

## 3. 首次使用

先构建并执行全部C++测试：

```bat
cd /d E:\openisac\OpenISAC-main
cpp_phy\build_windows_vs2019.cmd
```

运行不依赖VLC的逐字节完整链回环测试：

```bat
cpp_phy\test_video_channel_sim.cmd
```

看到 `PASS`、`FER 0.000%` 和输入/输出包数相同，说明UDP分片、PHY、信道、CRC和重组工作正常。

## 4. 一键播放

推荐使用新的手动参数入口。双击或在终端执行：

```bat
cpp_phy\manual_video_phy_test.cmd
```

命令会依次询问视频文件、Rank、调制方式、SNR、CFO、SFO、定时偏移、含多普勒的多径、图形刷新周期、感知相干帧数、发射相关系数、接收相关系数、Rank-4转码码率和导频模式。Rank-1/2/4都会打开前台VLC接收画面、C++物理层日志和Python实时监视器；Rank-4显示四层星座、对角4×4信道响应和16链路合并的距离–速度图。

Rank-1/2实时窗口包含：

- 两层有效载荷星座图（Rank-1时第二层标为未使用）；星座图只显示实际LDPC编码覆盖的QAM符号，帧尾固定标签0的填充RE不会再堆积在右上角；
- 一帧完整接收时域I/Q波形，默认最多4096点；
- H00/H01/H10/H11信道估计幅频响应；
- EVM、信道NMSE、频偏估计、SFO残余、定时估计、FER，以及2x2/4x4信道条件数中位值/P90和病态子载波比例；
- 由实际已知发送网格和接收网格得到的距离-速度热力图、CFAR目标标记、距离与径向速度。

Rank-4实时窗口把顶部扩展为四层星座；信道图显示H00/H11/H22/H33四条对角响应，CSV保留全部16条链路；距离–速度图使用16链路逐链变换后的功率合并结果。窗口同时显示4×4奇异值条件数的中位值/P90以及条件数大于10的有效子载波比例。

也可用完整位置参数直接启动而不询问：

```bat
cpp_phy\manual_video_phy_test.cmd "E:\openisac\视频源\1920x1080_2Mbps_2.mp4" 2 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2
```

Rank-4视频仿真示例：

```bat
cpp_phy\manual_video_phy_test.cmd "E:\openisac\视频源\1920x1080_2Mbps_2.mp4" 4 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.2 0.2 1000
```

启用统一DM-RS帧的Rank-4示例（最后一个参数为`nr-dmrs`）：

```bat
cpp_phy\manual_video_phy_test.cmd "E:\openisac\视频源\1920x1080_2Mbps_2.mp4" 4 64QAM 45 300 20 20 "0:0:0:0+3:-14:45:0+9:-8:-80:69.444444" 2 128 0.02 0.02 1000 nr-dmrs
```

位置参数依次为：视频、Rank、调制、SNR、CFO、SFO、定时偏移、TDL、图形快照周期、感知相干帧数、发射相关系数、接收相关系数、Rank-4转码码率kbit/s、导频模式。末尾两个参数都可省略，默认码率为1000 kbit/s、导频模式为`fdm`；导频可选`fdm`或`nr-dmrs`。相干帧数可选0、16、32、64、128、256、512，0关闭动态感知。Rank-4仿真计算速度低于Rank-2，因此脚本自动使用12个LDPC工作线程并转码限速，防止入口队列长期积压。

手动脚本在测试期间会保持命令窗口运行并管理它启动的全部子进程。出现以下任一情况时测试结束：

- 发送端播放完整个视频并退出；
- 用户关闭接收端VLC；
- 物理层桥接程序退出；
- 用户在手动脚本命令窗口按 `Ctrl+C`。

发送端正常播完后，脚本先等待物理层遥测至少静默3秒（或图形刷新周期加1秒，取较大值），让入口队列和VLC的500 ms网络缓存排空，避免截断视频尾部。随后脚本请求正常关闭，1.5秒内未退出时再强制关闭本次测试创建的发送VLC、接收VLC、Python实时监视器和物理层桥接器。它按进程ID清理，不会关闭用户在测试前已经打开的其他VLC实例。用户主动关闭接收VLC或按 `Ctrl+C` 时不等待排空，直接清理。

安装VLC后执行：

```bat
cpp_phy\start_vlc_video_demo.cmd "D:\video\sample.mp4" 1500 45
```

三个参数依次是视频文件、视频码率kbit/s和SNR dB。音频固定为128 kbit/s。程序会打开信道统计窗口、VLC接收窗口和VLC发送窗口。

推荐第一轮使用1500 kbit/s视频和45 dB SNR。确认稳定后逐步把SNR降低到40、36、32 dB，观察FER、画面块效应和停顿。

如果暂时不准备视频文件，可直接播放程序生成的10秒动态测试图案：

```bat
cpp_phy\start_vlc_test_pattern_demo.cmd 45
```

测试图案为320x180、15 fps、约800 kbit/s H.264。发送和接收VLC都强制使用独立实例，避免VLC把两端合并到同一进程。

## 5. 分别启动

先启动信道：

```bat
cpp_phy\run_video_channel_sim.cmd --snr 45 --cfo 300 --sfo 20 --tdl 0:0:0+3:-4:45+9:-8:-80 --tx-correlation 0.2 --rx-correlation 0.2
```

再启动接收VLC：

```bat
cpp_phy\start_vlc_video_receiver.cmd
```

最后启动发送VLC：

```bat
cpp_phy\start_vlc_video_sender.cmd "D:\video\sample.mp4" 1500
```

默认端口：VLC发送到50000，解调成功的数据发往VLC接收端50001。按 `Ctrl+C` 停止信道程序。

## 6. 信道参数

```text
--snr 45
--cfo 300
--sfo 20
--timing 20
--tdl 0:0:0+3:-4:45+9:-8:-80
--tx-correlation 0.2
--rx-correlation 0.2
--spatial-seed 49239
--rank 2
--modulation 64QAM
--pilot-mode nr-dmrs
--queue-packets 8192
--telemetry-dir measurement\live_phy_monitor
--telemetry-interval 2
--telemetry-points 4096
--sensing-coherent 128
--sensing-range-bins 128
```

`--rank` 接受1、2、4。Rank-1/2沿用原2x2动态链路，Rank-4切换到四发四收时域链；默认值仍为2，因此现有命令和脚本向下兼容。

`--pilot-mode`接受`fdm`和`nr-dmrs`，默认`fdm`保持旧命令及225 us帧不变。`nr-dmrs`在ZC后增加两个连续训练符号，SISO/Rank-1、2×2和4×4均使用同一五符号帧。Rank-1数据只发单层，但在双物理端口架构中仍探测两个Tx端口，便于后续Rank自适应；4×4把端口0/1和2/3放到两个交织频域梳齿，每个梳齿内用双符号时域OCC分离端口。数据符号保留原稀疏参考，用于噪声估计和前置CSI相位跟踪。该结构与NR思路相似，但不是逐比特兼容3GPP的PDSCH DM-RS实现。

TDL每条路径格式为 `时延采样:功率dB:初始相位度[:多普勒Hz]`，多条路径使用 `+` 连接。第四字段可省略，省略时为静态路径，因而完全兼容原三字段命令。最大路径时延必须小于128点CP。单站模型的速度换算为 `v = fd*c/(2*fc)`；5.8 GHz下69.444 Hz约为1.795 m/s，负值表示反向径向速度。

空间相关采用低复杂度 Kronecker 模型：每条TDL路径先生成独立的2x2复高斯矩阵，再按 `Rrx^(1/2) * W * Rtx^(1/2)` 引入收发两侧相关性。`--tx-correlation` 和 `--rx-correlation` 的绝对值必须小于1；0表示独立天线，0.2是推荐默认值，0.6用于明显相关场景，0.9接近病态阵列，只适合压力测试。`--spatial-seed` 控制可重复的信道 realization，相同TDL、相关系数和种子可复现实验。该实现仅涉及每条路径上的2x2小矩阵乘法，适合后续C++实时实现，不增加迭代求解。

当前视频入口没有 `--condition-number` 或“目标条件数”参数。Tx/Rx相关系数只能改变条件数的统计分布，不能精确指定监视器中显示的条件数；`--spatial-seed` 固定后可重复得到同一个信道实现，适合比较不同接收算法。若要精确扫描条件数，需要使用后续规划的受控奇异值诊断信道，而不是把相关系数误当成条件数。

监视器中的“条件数 中位/P90”用于判断空间流是否可分离。条件数越接近1越好；程序把条件数大于10的有效子载波记为病态子载波。高相关性或随机信道的小奇异值都会使空间信道趋于不可分，MMSE噪声增强加重，即使设置很高的SNR，EVM仍可能明显变差。这是Rank-2/4 MIMO信道可分离性限制，不是AWGN数值设置失效。Rank-1对此不敏感。

视频桥每个刷新周期只采集一个连续感知批次，其余时间不执行距离-多普勒FFT。默认128帧相干积累在`fdm`下为28.8 ms、多普勒bin间隔34.722 Hz、速度bin间隔0.897 m/s；在`nr-dmrs`下为48.0 ms、20.833 Hz和约0.539 m/s。两种模式的距离bin间隔均为9.759 m；静态杂波均值抑制和二维CA-CFAR默认开启。Rank-4先对16条Tx→Rx链路分别完成距离/多普勒变换，再进行功率非相干合并，不要求跨射频链相位校准。遥测文件增加 `sensing_range_doppler.csv` 和 `sensing_detections.csv`。

`constellation.csv` 只保存当前分片实际使用的星座符号。`status.csv` 中的 `constellation_valid_symbols` 是绘制点数，`constellation_padding_symbols_excluded` 是未绘制的帧尾填充数。短UDP分片不再因为大量标签0填充而在64-QAM右上角显示一团；该改动只影响诊断显示，不改变发送波形、LDPC、CRC或接收判决。

程序每两秒显示：UDP输入/输出/丢弃包数、输入/输出码率、PHY帧数、FER和每帧处理延迟。由于VLC自身还有转码和网络缓存，播放器端到端延迟通常大于PHY处理延迟。

FFT、CP和子载波间隔可在命令行显式写为 `--fft 1024 --cp 128 --subcarrier-spacing 15000`，但正式帧算法目前只接受这一组参数；传入其他数值会立即报错，而不会静默使用错误参数。

## 7. 当前限制

- Rank和调制方式已可手动选择，但尚未在视频过程中自动切换MCS。
- Rank-4已支持视频字节、四层星座、16条信道响应及距离–速度感知闭环，但仍是Windows离线信道仿真，不代表能在`fdm`的225 us或`nr-dmrs`的375 us空口帧周期内实时完成。
- STBC可以兼容，但必须作为独立发射模式实现。当前C++正式视频链路实现的是Rank-1/2/4空间复用；Python已有2发Alamouti STBC/SFBC参考实现，C++视频入口尚未宣称STBC可用。后续接口应使用独立的`mimo-mode=spatial-multiplexing/stbc`，STBC模式固定单数据流并采用两时隙Alamouti合并；不建议把4x4空间复用的Rank字段复用为STBC标志。
- UDP接收使用独立线程和8192包有界队列，图形窗口同时显示Socket接收量、队列丢包、当前深度和峰值。队列只吸收突发；如果平均输入长期超过PHY处理能力，延迟仍会持续增长。
- 当前没有ARQ。低SNR下CRC失败的数据报直接丢弃，符合低延迟视频验证需求。
- 当前输出距离和径向速度，尚未做阵列测角；TDL多普勒是帧间相位旋转的窄带目标模型，不模拟目标微动或路径出生/消失。
- VLC已在本机标准路径完成联调；脚本也会搜索PATH。

## 8. 当前实测

- 统一DM-RS兼容回归：Rank-1/2/4、64-QAM、45 dB、300 Hz CFO、20 ppm SFO、Tx/Rx相关系数0.02时，各30个UDP数据报均逐字节恢复，FER均为0；对应PHY帧数为126、72和60。Rank-4固定种子100帧对照中，`fdm`的EVM/预FEC BER/CSI NMSE/接收均值为6.562%/0.005047/-25.736 dB/838.996 us，`nr-dmrs`为7.082%/0.004446/-25.683 dB/986.250 us，两者均100/100 CRC通过。无CFO/SFO的30帧隔离测试中，梳齿+时域OCC DM-RS的EVM和CSI NMSE为5.823%和-45.982 dB，优于FDM的7.378%和-40.890 dB；实际20 ppm条件下前置CSI仍有时间老化，因此当前版本以兼容性和BER闭环为通过判据，不宣称EVM必然优于FDM。
- `nr-dmrs`保持每帧载荷不变，但帧由225 us增加到375 us，所以理论净空口吞吐为原`fdm`的60%。这是第一版保守结构；后续若去掉数据符号中的重复信道导频，可回收部分开销，但必须先保留相位跟踪和噪声估计能力。

- 新增Rank-4视频字节回环：Rank-4/64-QAM、45 dB、300 Hz CFO、20 ppm SFO、空间相关三径、12个LDPC线程，30个UDP数据报拆为60个PHY帧，30/30逐字节恢复，FER 0，平均仿真处理约3.892 ms/PHY帧。Rank-2与Rank-1兼容回归也分别通过20/20和10/10数据报，FER均为0。
- Rank-4视频感知综合回归：80个UDP数据报、160个PHY帧全部逐字节恢复，FER 0；128帧、16链路功率合并报告87.830 m、69.444 Hz、1.795 m/s，CFAR输出1个显示目标；四层星座各756点、时域波形3508点、16条信道CSV及Python监视器读取全部通过。

- VLC桌面捕获实播：74个MPEG-TS UDP包、148个PHY帧，FER 0、UDP丢包0。
- 动态I420/H.264测试图案：132个MPEG-TS UDP包、264个PHY帧，FER 0、UDP丢包0。
- 两轮均使用45 dB SNR、300 Hz CFO、20 ppm SFO和默认三径TDL。
- 36 dB扩展回环：50个UDP包、120个PHY帧，逐字节一致，FER 0；该短样本只作为工程回归，不代替长时间统计。
- `1920x1080_2Mbps_2.mp4`：26789个UDP包、53578个PHY帧，输入输出一致，FER 0、桥内丢包0，接收流水约0.390 ms/帧。
- `1920x1080_5Mbps_2.mp4` 初测：桥内47719个UDP包、95438个PHY帧全部通过，FER 0、桥内丢包0，接收流水约0.374 ms/帧；但VLC日志发现3859次TS连续计数跳变，证明旧版同线程UDP入口在高码率突发下发生统计范围外丢包。该结果判定为“PHY正确、端到端未通过”，并据此加入独立UDP接收队列；修复后的复测结果应另行记录，不能用桥内FER代替端到端连续性检查。
- 独立UDP接收队列修复后，同一5 Mbit/s源复测：Socket收到52983包，队列峰值6958/8192、队列丢包0；52983包全部恢复输出，105966个PHY帧FER 0，接收流水约0.367 ms/帧；VLC TS连续性错误为0。该样片端到端通过，但队列峰值已达到容量约85%，更高码率或更强突发应增大队列或继续优化发送/信道流水。
- 动态感知闭环：默认运动路径为9采样延迟、-8 dB、69.444 Hz。80个UDP数据报拆为192个Rank-2/64-QAM PHY帧后全部逐字节恢复，FER 0；128帧感知报告87.830 m、69.444 Hz、1.795 m/s，CFAR最强目标与注入参数一致。
- 星座填充回归：188字节短包对应672个有效64-QAM符号和1760个帧尾填充符号；CSV只输出672个有效点，右上角点比例由约45.6%恢复到1.49%，该帧CRC通过。短视频生命周期测试结束后，本次脚本创建的两个VLC、Python监视器和物理层桥接器进程全部自动退出。
- 空间相关扫频（Rank-2/64-QAM、45 dB、相同种子、收发相关系数取相同值）：平坦TDL下相关系数0/0.2/0.6/0.9对应EVM约0.91%/0.92%/1.55%/6.29%，条件数中位值约2.57/2.58/4.65/19.64；0.9时100%有效子载波条件数大于10。默认动态三径、相关系数0.2时EVM约2.60%、条件数中位/P90约2.77/4.27，FER 0。结果确认相关性参数和病态信道诊断方向正确。
- 空间相关综合回归（Rank-2/64-QAM、45 dB、Tx/Rx相关系数0.2、300 Hz CFO、20 ppm SFO）：80个UDP包、192个PHY帧全部逐字节恢复，FER 0；快照EVM 2.02%、信道NMSE -26.09 dB、条件数中位/P90为2.78/4.21、病态子载波0%；感知峰值仍为87.830 m、1.795 m/s。

## Rank-4 EVM低复杂度优化

Rank-4接收端默认对两个已经完成相位对齐的数据OFDM符号的CSI做帧内相干平均，并把导频残余噪声用于MMSE时采用0.5倍正则化系数。可用视频桥参数 `--mmse-scale VALUE` 或诊断程序参数 `--mmse-scale VALUE --intra-frame-average 0|1` 做固定种子对照。手动视频脚本已显式传入 `--mmse-scale 0.5`。

在Rank-4/64-QAM、45 dB、300 Hz CFO、20 ppm SFO、Tx/Rx相关系数0.02和相同三径TDL下，500帧串行VS2019 Release对照如下：未优化EVM 6.938%、预FEC BER 0.005709；优化后EVM 6.605%、预FEC BER 0.005088，500/500帧CRC全部通过。EVM相对改善约4.8%，BER改善约10.9%，接收均值从810.9 us增加到824.5 us。

同条件视频快照中，Rank-2的EVM约2.29%、条件数中位/P90约2.75/4.41、病态子载波0%；Rank-4的EVM约5.58%、条件数中位/P90约7.71/15.71、病态子载波约32.25%。Rank-4保持总发射功率不变，因此每层功率比Rank-2低3 dB，并且每层要分离另外三个空间层，这是两者EVM差距的主要原因。Rank-2仿真入口使用已知SNR噪声方差，而Rank-4使用导频残余估计，也会造成统计口径上的次要差异。
