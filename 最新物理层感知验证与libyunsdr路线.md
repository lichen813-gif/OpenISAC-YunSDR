# 最新物理层感知验证与 libyunsdr 路线

## 1. 目标调整

最终硬件接口调整为 `libyunsdr`，不再接入 USRP/UHD。当前阶段只完成两件事：

1. 验证原 OpenISAC 感知代码是否可运行，并确认它与当前正式物理层的差异；
2. 建立支持当前 1024/128、2×2 Rank-1/2、高阶QAM物理层的硬件无关感知核心。

本阶段不绑定任何射频SDK。后续 `libyunsdr` 只负责收发IQ、时间戳和设备状态，算法核心继续保持Windows/Linux可编译。

## 2. 原感知代码验证结论

原 Python 感知仿真可运行。使用 `config/BS_Sim.yaml`、35 dB、目标50 m/2 m/s、2帧、单接收天线完成距离-速度处理，检测结果约52.164 m和2.033 m/s。感知显示程序的5项测试也全部通过。

原算法不能直接接到当前正式帧，原因是原信道估计核心采用发送符号共轭乘接收符号，隐含发送符号为单位幅度QPSK。当前物理层支持16/64/256-QAM和Rank-2空间复用，发送符号幅度变化以及另一发射流都会进入估计误差。因此保留原程序作为历史基线和显示参考，不把它直接作为新物理层感知核心。

## 3. 当前正式物理层边界

| 参数 | 当前值 |
|---|---:|
| FFT / CP | 1024 / 128 |
| 帧结构 | 1个ZC前导 + 2个数据OFDM符号 |
| 物理发射/接收端口 | 2 / 2 |
| Rank和调制 | Rank-1/2，QPSK、16/64/256-QAM |
| 数据/梳状导频/相位参考 | 672 / 216 / 8个RE位置 |
| 子载波间隔 | 15 kHz |
| 采样率 | 15.36 MS/s |
| 帧周期 | 225 us |
| 编码 | CRC-16 + LDPC(1008,504) |

感知入口位于接收同步、CFO/SFO校正、去CP和FFT之后。输入是本地已知发送频域网格和接收频域网格，因此以后更换射频设备不会改变感知算法。

## 4. 新感知算法

### 4.1 2×2已知波形信道估计

每个子载波使用两个数据OFDM符号，建立：

```text
y[0] = h0*x0[0] + h1*x1[0]
y[1] = h0*x0[1] + h1*x1[1]
```

对满秩资源单元执行带轻量对角加载的2×2 LS，恢复指定 `Tx -> Rx` 通道。只有一个发射端口有参考的导频/控制资源单元直接相除。病态资源单元和FDM空洞在有效带内用相邻有效估计线性插值。

此方案的工程特点：

- 支持非单位幅度的QPSK、16/64/256-QAM；
- 能从Rank-2双发叠加回波中分离指定发射端口；
- 每个子载波只做解析2×2运算，不使用迭代求解；
- 复用输出、掩码和插值缓冲，适合后续C++实时运行；
- 可选择任意一个2×2 `Tx -> Rx` 通道，后续可扩展为多通道联合和角度估计。

### 4.2 距离-多普勒处理

每帧形成一次指定通道的频率响应。有效子载波加Hamming窗并做1024点IFFT得到距离像；连续64帧加Hamming窗并沿慢时间做64点FFT，再进行多普勒FFT shift，得到复数距离-多普勒图。

默认参数对应：

| 指标 | 数值 |
|---|---:|
| 相干帧数 / 相干时间 | 64 / 14.4 ms |
| 距离FFT / 多普勒FFT | 1024 / 64 |
| 距离轴bin间隔 | 9.7589 m |
| 理想无模糊距离 | 9993.08 m |
| 多普勒轴bin间隔 | 69.4444 Hz |
| 速度轴bin间隔（5.8 GHz） | 1.7947 m/s |
| 理想无模糊速度（5.8 GHz） | 约±57.43 m/s |

上述按单站往返传播定义。距离轴bin间隔只是采样间隔，不等于考虑实际占用带宽、窗函数和信道后的目标分辨率。

### 4.3 连续采集保护

感知处理器记录64位采集序号和时间戳。发现序号缺口或时间戳回退时，丢弃当前不完整相干批次并重新积累，防止把不连续帧错误地当作均匀慢时间采样。

### 4.4 静态杂波抑制与多目标CA-CFAR

可选静态杂波抑制在多普勒窗和FFT之前，对每个子载波减去64帧复均值。它适合抑制直达泄漏和固定反射，也会抑制真正的零速度目标，因此保留可关闭配置。

二维CA-CFAR采用积分图计算训练窗均值，不对每个检测单元重复遍历完整邻域。默认训练窗为多普勒3、距离4，保护窗均为1，虚警概率 `1e-5`；候选点按功率排序并做2×2 bin非极大值抑制。默认检测范围为循环距离IFFT的非负延迟半区，避免报告负延迟镜像。

## 5. 验证结果

VS2019 v142/x64 Release回归包括：

1. 使用真实Rank-2/256-QAM正式发送网格，在无噪声双发回波下验证LS信道估计；直接估计误差NMSE低于 `1e-7`；
2. 使用64个不同载荷和导频种子的Rank-2/64-QAM正式帧，加入双发回波和复高斯噪声；
3. 注入距离bin 5、多普勒bin +1，输出距离bin 5、FFT-shift后多普勒bin 33；
4. 物理结果为48.794 m和1.795 m/s；
5. 人为跳过一个采集序号后，相干批次被清空，并能从新序号重新完成结果。

每轮50个相干批次的两次本机性能复测结果：

| 指标 | Release结果 |
|---|---:|
| 普通帧信道估计与积累 | 9.380–9.896 us/帧 |
| 最后一帧含完整距离-多普勒变换 | 1605.006–1653.402 us |
| 64帧批次总时间 | 2197.338–2278.412 us |
| 摊销时间 | 34.333–35.600 us/帧 |
| 直接估计 / 插值子载波 | 786 / 109 |

上表是加入目标检测前的基础记录。最新多目标场景包含一个幅度更强的零多普勒静态反射，以及两个运动目标。开启杂波抑制后，CA-CFAR只保留：

| 目标 | 距离 | 速度 | CFAR门限余量 |
|---|---:|---:|---:|
| 运动目标1 | 48.794 m | 1.795 m/s | 33.6 dB |
| 运动目标2 | 175.660 m | -5.384 m/s | 33.2 dB |

两次50批最新Release复测：

| 指标 | 结果 |
|---|---:|
| 普通帧信道估计与积累 | 9.307–9.702 us/帧 |
| 最后一帧含二维变换、杂波抑制和CA-CFAR | 1689.210–1767.370 us |
| 64帧批次总时间 | 2276.918–2380.026 us |
| 摊销时间 | 35.577–37.188 us/帧 |
| CA-CFAR测试单元 | 28392 |
| 最终检测目标数 | 2 |

运行命令：

```bat
cpp_phy\run_cpp_sensing_benchmark.cmd 50
cpp_phy\run_cpp_sensing_plot.cmd 50
```

第二条命令输出 `measurement/cpp_sensing_plot/range_doppler.csv`、`detections.csv`、`summary.csv` 和 `range_doppler.png`。数值全部由C++计算，Python只读取CSV并绘制距离-多普勒热图、距离像、多普勒像和检测标记。

另有完整时域共享前端回归：Rank-2/64-QAM发送经过两径TDL（零时延静态参考和延迟5点运动目标）、48 dB AWGN、300 Hz CFO和20 ppm SFO，再经过ZC同步、去CP、FFT和SFO校正；同一接收网格同时完成通信和感知。64/64帧通信CRC通过，感知输出距离bin 5、多普勒bin +1并通过CA-CFAR。

核心文件：

- `cpp_phy/include/openisac/sensing.hpp`
- `cpp_phy/src/sensing.cpp`
- `cpp_phy/tests/test_main.cpp`
- `cpp_phy/benchmarks/bench_sensing.cpp`

## 6. 当前限制

- 当前已有离线Python图，但尚未接入真实设备连续刷新；
- 尚未做多接收通道联合、到达角估计和阵列标定；
- 中心频率和帧周期可配置，但真实硬件轴标定仍需使用实际 `libyunsdr` 配置与时间戳验证；
- CA-CFAR当前是均值型背景估计；强非均匀杂波边缘场景后续可再比较OS-CFAR，但不作为当前工程默认。

## 7. libyunsdr 接入边界

不在感知代码中直接调用 `libyunsdr`。后续建立一个薄适配器，职责仅限：

1. 配置设备、采样率、中心频率、增益、天线/通道和流格式；
2. 将接收IQ填入现有固定容量采集环形缓冲；
3. 将硬件时间戳转换为统一64位时间线，同时生成连续采集序号；
4. 把溢出、欠载、超时和时间戳不连续映射为现有统计与感知重置事件；
5. 从正式发射网格保留本地感知参考，不依赖接收端猜测发送数据。

SDK层和算法层之间的数据结构继续使用 `complex<float>` IQ、`DynamicLinkCaptureFrame` 和频域网格。已核对官方 [v3tech/libyunsdr](https://github.com/v3tech/libyunsdr) 的2026.2公开头文件：它包含设备开关、RX/TX采样率与本振设置、多端口读写、时间戳以及overflow/underflow事件计数接口。当前工作区仍缺目标设备Windows导入库、DLL、驱动和厂家样例，不能进行可链接的硬件回归；具体样本布局、调用标志和设备URL以随设备交付的SDK版本为准。

## 8. 后续阶段

1. 取得目标 `libyunsdr` SDK版本、头文件、Windows导入库/DLL、驱动和最小双通道收发样例；
2. 实现连续IQ流分帧及Windows薄适配器，先用模拟SDK/文件IQ验证时间戳和事件映射；
3. 做线缆加衰减器回环，核对采样格式、通道顺序、时间戳单位、TX/RX定时和溢出计数；
4. 对比文件IQ与 `libyunsdr` 实采结果，再测试静止目标与1–2 m/s低速目标；
5. 在2×2稳定后扩展多接收通道联合和角度估计，再评估4×4/8×8感知资源网格。
